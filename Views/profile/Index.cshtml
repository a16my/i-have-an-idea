@model IEnumerable<BirFikrimVar.Models.Post>
@{
    ViewData["Title"] = "My Profile";
}

<h2>@ViewBag.UserFullName's Profile</h2>

@if (!Model.Any())
{
    <p>You haven’t posted anything yet.</p>
}
else
{
    @foreach (var post in Model)
    {
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">@post.Title</h5>
                <p class="card-text">@post.Content</p>
                <p><small>Posted on @post.CreatedAt.ToLocalTime()</small></p>

              <p>
    ❤️ Likes: @post.Likes.Count  
    💬 Comments: @post.Comments.Count
</p>

<p>
 <a asp-action="Edit" asp-route-id="@post.Id" class="btn btn-warning">Edit</a>
<a asp-action="Delete" asp-route-id="@post.Id" class="btn btn-danger">Delete</a></p>

            </div>
        </div>
    }
}